NOTES: NO SOURCE, JUST NOTES FOR ME
---------------------------------------------------------------------------------------
TODOS:
-investigate brownian motion: probably just a feature of pbf...
-morton codes
	-implemented, but no efficiency gain?
-liquid surface rendering
-merge fields in the particle struct that never live together: probably not a lot to gain
-adaptive pbf article imrovements
-better spatial hash, multi level memory article
-better sorting, eventually radix sort
-separate the simulation domain and the bounding box
-completely disjoin the graphics fps from the simulation timestep:
	-make it possible to have several physics steps per graphics frame
	-make it possible for several graphics frames to pass without a physics step
-cellCount is used for two things now: maybe change that, so we don't need as many uav barriers?
-could we achieve a speedup by not having a separate copy pass where we copy the sorted particles
 to the original, and instead just re-binding the buffers?
-make the descriptor heap's layout defined in 1 place, such that if I change
 descriptor layout, I don't have to change it in several places, just 1
-make it possible to have several physics steps per graphics frame
-make it possible for several graphics frames to pass without a physics step
-investigate whether we're actually double buffering  
-double buffer the sorting, that is to say, instead of copying the sorted particles back to the original 
 buffer, just re-bind the buffers, so that the sorted buffer becomes the original and vice versa
-pre-compute neighboring cell offsets, each time we call NeighborCellIndices, we compute the same indices, 
 so we could pre-compute them and just add them to the cell index
-check bottleneck in nsight graphics
-check if we can enforce fps cap better (it's a bit too far under the target)